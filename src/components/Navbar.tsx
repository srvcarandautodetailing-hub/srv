// Navigation is now provided globally by src/components/layout/Header.tsx
// via src/app/layout.tsx. This stub prevents double-render from page templates
// that still import the old Navbar component.
export function Navbar() {
  return null;
}
