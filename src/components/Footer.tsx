// Footer is now provided globally by src/components/layout/Footer.tsx
// via src/app/layout.tsx. This stub prevents double-render from page templates
// that still import the old Footer component.
export function Footer() {
  return null;
}
